{
	"name": "Run bar ms on Docker",

	"dockerComposeFile": ["../docker-compose.yml"],

	"service": "bar",

	"runServices" : ["bar"],

	"workspaceFolder": "/workspace",

	"extensions": ["ms-python.python"],

	"postCreateCommand": "pip install -r requirements-test.txt"
}